[project]
name = "wallet-inspector-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.9.2",
    "python-dotenv>=1.1.0",
    "tabulate>=0.9.0",
]
